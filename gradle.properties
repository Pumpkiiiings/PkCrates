# The Java version used to compile is pinned by the `toolchain` block in
# build.gradle.kts, which Gradle resolves per machine. Do not set
# `org.gradle.java.home` here — an absolute path breaks every other machine and CI.

org.gradle.parallel=true
org.gradle.caching=true
